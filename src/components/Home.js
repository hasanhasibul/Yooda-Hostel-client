import React from 'react';
import DefaultLayout from './DefaultLayout';

const Home = () => {
    return (
        <DefaultLayout>
            <h2>Welcome to admin dashboard</h2>
        </DefaultLayout>
    );
};

export default Home;